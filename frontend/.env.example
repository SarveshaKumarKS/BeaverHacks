NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: enables provider TTS (OpenAI voices via backend /tts endpoint)
# Set to the same base URL as NEXT_PUBLIC_API_URL if OPENAI_API_KEY is configured on the backend
# Leave blank to use browser speechSynthesis fallback
NEXT_PUBLIC_TTS_ENDPOINT=
